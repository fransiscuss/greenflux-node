# Used only by `npm run smoke`; never commit real values.
GREENFLUX_PLATFORM_BASE_URL=https://platform.greenflux.com/
GREENFLUX_PLATFORM_TOKEN=
# Optional: check a known location instead of listing locations.
GREENFLUX_LOCATION_ID=
